# Board diagrams

Generated by [make_figures.py](make_figures.py), which replays the exact
move sequences quoted in the paper and renders vector PDF via
python-chess's SVG module:

```bash
uv run --with cairosvg python paper/figures/make_figures.py
```

## Provenance and licensing

The chess **piece images** embedded in these PDFs come from python-chess's
SVG renderer and are by **Colin M. L. Burnett**, triple-licensed under the
GFDL, BSD and GPL (see the
[python-chess SVG documentation](https://python-chess.readthedocs.io/en/latest/svg.html)).
They are used here under the **BSD option** of that triple license, with
this attribution. The board rendering code is python-chess's (GPL-3.0+);
these output files are therefore **excluded from the repository's MIT
license** and carry the piece set's terms instead. The generator script
`make_figures.py` itself is MIT like the rest of the repository.
